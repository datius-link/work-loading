// Unused — the app dropped react-router in favor of single-URL, state-based
// navigation (see App.jsx: booting/isAuthenticated gate the whole screen
// directly, no route guard needed). Kept only because this sandbox can't
// delete files on the mounted drive; safe to delete this file by hand.
export {};
